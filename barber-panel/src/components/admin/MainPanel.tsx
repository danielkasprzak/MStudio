import { Outlet } from "react-router-dom"

export default () => {
    return (
        <div className='relative w-full h-full flex flex-col justify-start'>
            {/* <Outlet /> */}
        </div>
    )
}