import { Outlet } from "react-router-dom"

export default () => {
    return (
        <div className='relative w-full h-full flex flex-col justify-start py-8 ml-4'>
            {/* <Outlet /> */}
        </div>
    )
}